/**
 * Agent-related constants
 */

/** Storage key prefix for agent private keys in AsyncStorage */
export const AGENT_STORAGE_PREFIX = 'hl-agent:private-key:';

/** Default name for the Riverrun Agent */
export const DEFAULT_AGENT_NAME = 'Riverrun Agent';

/** Maximum number of named agents allowed per account */
export const MAX_NAMED_AGENTS = 3;
